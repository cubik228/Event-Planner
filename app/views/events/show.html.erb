<div class="container my-4">
  <p id="notice" class="alert alert-info"><%= notice %></p>

  <div class="card">
    <div class="card-body">
      <h2 class="card-title"><%= @event.name %></h2>

      <dl class="row">
        <dt class="col-sm-3">Date:</dt>
        <dd class="col-sm-9"><%= @event.date %></dd>

        <dt class="col-sm-3">Description:</dt>
        <dd class="col-sm-9"><%= @event.description %></dd>

        <dt class="col-sm-3">Category:</dt>
        <dd class="col-sm-9"><%= @event.category.name %></dd>
      </dl>

      <% if @weather_data.present? && @weather_data['current'].present? %>
        <h3 class="mt-4">Погода в <%= @weather_data['location']['name'] %></h3>
        <p>Текущая температура: <%= @weather_data['current']['temp_c'] %> градусов Цельсия</p>
      <% elsif @weather_data.present? && @weather_data['forecast'].present? %>
        <h3 class="mt-4">Прогноз погоды в <%= @weather_data['location']['name'] %></h3>
        <% @weather_data['forecast']['forecastday'].each do |forecastday| %>
          <h4 class="mt-3"><%= forecastday['date'] %></h4>
          <p>Максимальная температура: <%= forecastday['day']['maxtemp_c'] %> градусов Цельсия</p>
        <% end %>
      <% else %>
        <p class="mt-4">Данные о погоде недоступны.</p>
      <% end %>

      <%= link_to 'Edit', edit_event_path(@event), class: 'btn btn-primary' %>
      <%= link_to 'Back', events_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>
  